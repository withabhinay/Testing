import { LogMethod, Logger, createLogger, lastSequenceNumber, resetSequence } from 'bs-logger/dist/logger';
import { LogContext, LogContexts } from 'bs-logger/dist/logger/context';
import { LogLevelName, LogLevels, logLevelNameFor, parseLogLevel } from 'bs-logger/dist/logger/level';
import { LogMessage, LogMessageFormatter, LogMessageTranslator, registerLogFormatter, resetLogFormatters } from 'bs-logger/dist/logger/message';
import { rootLogger, setup } from 'bs-logger/dist/logger/root';
import { DEFAULT_LOG_TARGET, LogTarget, parseLogTargets } from 'bs-logger/dist/logger/target';
import * as testing from 'bs-logger/dist/testing';
export { rootLogger as default, rootLogger as logger, setup, createLogger, DEFAULT_LOG_TARGET, lastSequenceNumber, LogContext, LogContexts, Logger, LogLevelName, logLevelNameFor, LogLevels, LogMessage, LogMessageFormatter, LogMessageTranslator, LogMethod, LogTarget, parseLogLevel, parseLogTargets, registerLogFormatter, resetLogFormatters, resetSequence, testing, };
